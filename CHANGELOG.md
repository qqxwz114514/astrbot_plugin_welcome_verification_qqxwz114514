# 1.0.2 - 2026-xx-xx

## ✨ 新增

- **验证失败立即禁言**：3 次机会用完后（仅处理失败时）立即禁言该用户，防止等待窗口内刷屏
  - 二级验证路径：禁言时长 = `secondary_verification_timeout`（秒）+ 120 秒
  - 超时踢人路径：禁言时长 = `timeout_kick_delay`（秒）+ 120 秒
- **管理员处理解禁**：`wv pass` 同意入群 → 解除禁言恢复正常发言；`wv cancel` 取消踢人 → 同样解除禁言
- **撤回兜底**：验证失败等待期内继续撤回该用户消息（不扣次数），防止禁言调用失败

## 🐛 修复

- 修复机会用完后撤回失效的问题（`_check_answer` 对失败等待期状态早退）
- 启用原未使用的 `verification_ban_message` 配置作为禁言通知文案

# 1.0.1 - 2026-xx-xx

## 🐛 修复

- **非数字输入计入尝试次数**：入群验证（数学题模式）期间，成员输入非数字内容（如广告、乱码）不再只警告“请输入数字答案”，而是与答错同等处理，扣除一次机会；次数耗尽后按原有逻辑进入二级验证/踢人，防止广告刷屏无限消耗

# 2.8.1 - 2026-06-18

## 更改仓库地址


# 2.8.0 - 2026-05-30

## 🚀 重要更新

### 统一指令系统
所有命令统一为 `wv` 前缀，告别分散的 `/pass`、`/kick`、`/cancel_kick`、`welcome` 等命令：
- `wv welcome` - 查看/设置群组欢迎配置
- `wv pass @用户` - 允许入群
- `wv kick @用户` - 踢出用户
- `wv cancel @用户` - 取消踢人
- `wv` - 显示完整命令帮助

### Bug 修复
- **修复负数答案验证问题**：`isdigit()` 无法识别负号，导致正确答案为负数时用户输入 `-4` 被错误拦截，改用 `try int()` 转换
- **修复命令误触发**：普通消息以 `welcome` 开头不再被误判为指令
- **修复锁内 I/O 问题**：`/pass` 指令的 `await event.send()` 在锁内部执行，降低并发性能
- **修复踢人不清理状态**：被踢用户的主验证任务仍在后台运行空转，现在踢人时清理所有关联状态
- **修复后台任务异常吞没**：`asyncio.create_task` 未绑定异常回调，静默吞掉运行时异常

### 性能与健壮性优化
- **优化群成员检查**：`_is_member_in_group` 改用 `get_group_member_info` 单成员查询，替代拉取整个群成员列表
- **简化随机出题**：从双层运算符表达式简化为单运算符，保证一次生成合法题目
- **添加异常保护**：验证流程中的 `event.send` 添加 try/except 保护，防止踢人后发送失败引发异常
- **任务生命周期管理**：新增 `verification_tasks` 字典追踪主验证任务，支持 `terminate` 时完整清理

# 2.7.0 - 2026-05-24

## 重大功能更新：群组自定义配置

### 新增功能

**1. 群组自定义欢迎配置 ✨**
- 支持为不同群组设置独立的欢迎文本
- 支持为不同群组设置独立的欢迎图片
- 支持为不同群组设置独立的题库
- 配置优先级：群组配置 > 全局配置

**2. WebUI 可视化配置 🎨**
- 使用 AstrBot 的 template_list 类型
- 在 WebUI 配置页面中可直接添加、编辑、删除群组配置
- 每个群组可独立配置：欢迎文本、图片开关、图片URL、题库

**3. 群内命令配置 ⚡**
- 新增 `welcome` 系列命令
- 支持通过命令快速查看和修改群组配置
- 配置自动同步到 WebUI

### 功能优化

**踢人命令增强 🔧**
- `/kick` 命令现在任何时候都能使用（只要是管理员或群主）
- 不再局限于二级验证阶段，可随时踢出任意群员
- 保留了管理员权限检查，确保只有管理员和群主可以使用
- 添加了防止踢出自己的保护

### 命令列表

| 命令 | 说明 | 权限 |
|------|------|------|
| `welcome` | 查看当前群组配置 | 所有人 |
| `welcome text <内容>` | 设置欢迎文本 | 管理员/群主 |
| `welcome image <路径>` | 设置欢迎图片 | 管理员/群主 |
| `welcome image on/off` | 启用/禁用图片 | 管理员/群主 |
| `welcome reset` | 重置为全局配置 | 管理员/群主 |
| `/kick @用户` | 踢出任意用户 | 管理员/群主 |

---

# 历史版本

## 2.6.0 - 2026-05-16

### 主要修复

**1. 入群事件处理终于正常了！**
- 之前插件把群里所有消息都当成新成员入群来处理，现在不会了
- 现在只有真正有人进群的时候才会触发欢迎和验证

**2. 命令不会乱匹配了**
- 之前 `/pass` 命令会错误匹配 `/password` 这种消息
- 现在只有输入完全匹配的命令才会生效

**3. 不会卡死了**
- 修复了一个可能导致插件卡死的问题
- 之前验证成功时偶尔会卡住不动

**4. 用户昵称显示正确了**
- 之前有时候显示的是用户ID而不是昵称
- 现在会正确显示用户的群名片或昵称

**5. 踢人功能修复**
- 二次验证超时后踢人功能现在正常工作
- 检查机器人有没有管理员权限，如果没有会通知管理员手动处理

**6. 代码质量提升**
- 日志输出更简洁，不会一堆重复的信息
- 修复了一些潜在的问题

## 2.5.4 及之前

- 修复了一些已知问题
- 添加了默认的欢迎图片 API
- 曾经因为严重 Bug 回退过版本
